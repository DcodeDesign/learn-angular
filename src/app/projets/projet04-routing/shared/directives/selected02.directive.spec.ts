import { Selected02Directive } from './selected02.directive';

describe('SelectedDirective', () => {
  it('should create an instance', () => {
    const directive = new Selected02Directive();
    expect(directive).toBeTruthy();
  });
});
